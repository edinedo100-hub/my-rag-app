rules_version = '2';
service firebase.storage {
  match /b/{bubble}/o {
    // Authenticated users can upload bills to their own directory
    match /bills/{userId}/{allPaths=**} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
    
    // (Manual admin review happens in the console)
  }
}
